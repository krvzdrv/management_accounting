# Инструкция по установке системы управленческого учета

## Пошаговая инструкция по установке всех скриптов

### Шаг 1: Создание Google Таблицы

1. Откройте [Google Sheets](https://sheets.google.com)
2. Нажмите **+** или **Пустой файл** для создания новой таблицы
3. Дайте таблице название, например: **"Управленческий учет Alumineu"**
4. Сохраните таблицу

### Шаг 2: Открытие редактора скриптов

1. В меню Google Таблицы выберите **Расширения** → **Apps Script**
2. Откроется редактор Google Apps Script в новой вкладке
3. Вы увидите файл по умолчанию с функцией `myFunction`

### Шаг 3: Удаление файла по умолчанию

1. В левой панели редактора найдите файл (обычно называется "Код.gs" или "Code.gs")
2. Нажмите на три точки (⋮) рядом с файлом
3. Выберите **Удалить**
4. Подтвердите удаление

### Шаг 4: Создание файлов модулей

Вам нужно создать **10 файлов** со скриптами. Для каждого файла:

1. Нажмите кнопку **+** (плюс) в левой панели редактора
2. Выберите **Скрипт**
3. Переименуйте файл (нажмите на название файла или используйте три точки → Переименовать)
4. Скопируйте код из соответствующего файла `.gs` в этот проект

#### Список файлов для создания:

**Файл 1: Config**
- Название файла: `Config`
- Скопируйте весь код из файла `Config.gs`

**Файл 2: Utils**
- Название файла: `Utils`
- Скопируйте весь код из файла `Utils.gs`

**Файл 3: CurrencyManager**
- Название файла: `CurrencyManager`
- Скопируйте весь код из файла `CurrencyManager.gs`

**Файл 4: VATCalculator**
- Название файла: `VATCalculator`
- Скопируйте весь код из файла `VATCalculator.gs`

**Файл 5: PaymentManager**
- Название файла: `PaymentManager`
- Скопируйте весь код из файла `PaymentManager.gs`

**Файл 6: DebtCalculator**
- Название файла: `DebtCalculator`
- Скопируйте весь код из файла `DebtCalculator.gs`

**Файл 7: Notifications**
- Название файла: `Notifications`
- Скопируйте весь код из файла `Notifications.gs`

**Файл 8: Triggers**
- Название файла: `Triggers`
- Скопируйте весь код из файла `Triggers.gs`

**Файл 9: TestDataLoader**
- Название файла: `TestDataLoader`
- Скопируйте весь код из файла `TestDataLoader.gs`

**Файл 10: Main**
- Название файла: `Main`
- Скопируйте весь код из файла `Main.gs`

### Шаг 5: Сохранение проекта

1. После создания всех файлов нажмите **Сохранить** (💾) или `Ctrl+S` / `Cmd+S`
2. В верхней части редактора нажмите на название проекта (по умолчанию "Безымянный проект")
3. Переименуйте проект в: **"Management Accounting System"** или **"Управленческий учет"**
4. Нажмите **Сохранить** еще раз

### Шаг 6: Первый запуск и разрешения

1. В редакторе скриптов в верхней части выберите функцию `initializeSystem` из выпадающего списка функций (рядом с кнопкой "Выполнить")
2. Нажмите кнопку **Выполнить** (▶️)
3. Появится окно с запросом разрешений:
   - Нажмите **Проверить разрешения**
   - Выберите свой Google аккаунт
   - Появится предупреждение "Приложение не проверено"
   - Нажмите **Дополнительно** (Advanced)
   - Нажмите **Перейти к [название проекта] (небезопасно)** (Go to [project name] (unsafe))
   - Нажмите **Разрешить** (Allow)
4. Скрипт выполнится и создаст все необходимые таблицы

### Шаг 7: Проверка создания таблиц

1. Вернитесь в Google Таблицу (вкладка с таблицей)
2. Внизу должны появиться новые листы:
   - Currencies
   - Exchange_Rates
   - VAT_Rates
   - Companies
   - Counterparties
   - Products
   - Expense_Categories
   - Purchase_Orders
   - Purchase_Order_Lines
   - Sales_Orders
   - Sales_Order_LINES
   - Expenses
   - Cash_Transactions
   - Payments
   - Intercompany_Loans
   - Intercompany_Loan_Payments
   - Account_Balances

### Шаг 8: Загрузка тестовых данных (опционально)

1. Вернитесь в Google Таблицу
2. Обновите страницу (F5)
3. В меню должно появиться **💼 Управленческий учет**
4. Выберите **💼 Управленческий учет** → **🧪 Тестирование** → **Загрузить тестовые данные**
5. Подтвердите загрузку
6. Данные будут добавлены во все таблицы

### Шаг 9: Настройка триггеров

1. В меню **💼 Управленческий учет** → **⚙️ Настройки** → **Установить триггеры**
2. Или запустите функцию `setupAllTriggers()` из редактора скриптов
3. Триггеры настроятся автоматически для ежедневных задач

## Визуальная схема структуры проекта

```
Google Apps Script Проект
├── Config.gs              (Конфигурация)
├── Utils.gs               (Вспомогательные функции)
├── CurrencyManager.gs     (Управление валютами)
├── VATCalculator.gs      (Расчеты НДС)
├── PaymentManager.gs     (Управление платежами)
├── DebtCalculator.gs      (Расчет задолженностей)
├── Notifications.gs       (Уведомления)
├── Triggers.gs            (Триггеры)
├── TestDataLoader.gs      (Тестовые данные)
└── Main.gs                (Главное меню)
```

## Проверка правильности установки

### ✅ Чек-лист установки:

- [ ] Все 10 файлов созданы в редакторе скриптов
- [ ] Каждый файл содержит соответствующий код
- [ ] Проект сохранен
- [ ] Функция `initializeSystem()` выполнена успешно
- [ ] Все таблицы созданы в Google Таблице
- [ ] Меню "💼 Управленческий учет" появилось в Google Таблицах
- [ ] Тестовые данные загружены (опционально)
- [ ] Триггеры установлены

## Решение проблем

### Проблема: "Функция не найдена"

**Решение:**
- Убедитесь, что все файлы созданы и содержат код
- Проверьте, что файлы сохранены (Ctrl+S)
- Обновите страницу редактора скриптов

### Проблема: "Sheet not found"

**Решение:**
- Убедитесь, что функция `initializeSystem()` выполнена успешно
- Проверьте, что все таблицы созданы
- Запустите `initializeSystem()` еще раз

### Проблема: Меню не появилось

**Решение:**
- Обновите страницу Google Таблицы (F5)
- Убедитесь, что файл `Main.gs` создан и содержит функцию `onOpen()`
- Откройте таблицу заново

### Проблема: Ошибка разрешений

**Решение:**
- Убедитесь, что вы нажали "Разрешить" во всех окнах
- Проверьте, что используете правильный Google аккаунт
- Попробуйте выполнить функцию еще раз

### Проблема: Ошибка при загрузке тестовых данных

**Решение:**
- Убедитесь, что сначала выполнена `initializeSystem()`
- Проверьте, что все таблицы созданы
- Посмотрите логи в редакторе скриптов (Вид → Логи)

## Дополнительные советы

### Просмотр логов

1. В редакторе скриптов выберите **Вид** → **Логи**
2. Выполните функцию
3. Логи покажут все сообщения и ошибки

### Отладка

1. Установите точку останова (breakpoint) в коде
2. Выберите функцию для выполнения
3. Нажмите **Отладка** (🐛) вместо "Выполнить"
4. Код остановится на точке останова

### Резервное копирование

1. В редакторе скриптов: **Файл** → **Создать копию**
2. Или экспортируйте проект: **Файл** → **Скачать** → **.json**

## Следующие шаги после установки

1. ✅ Добавьте реальные данные компаний в лист **Companies**
2. ✅ Добавьте реальных контрагентов в лист **Counterparties**
3. ✅ Добавьте товары в лист **Products**
4. ✅ Обновите курсы валют в листе **Exchange_Rates**
5. ✅ Начните вводить реальные операции

## Поддержка

Если возникли проблемы:
1. Проверьте логи в редакторе скриптов
2. Убедитесь, что все файлы созданы правильно
3. Проверьте, что код скопирован полностью без ошибок
4. Убедитесь, что все разрешения предоставлены

---

**Готово!** Система установлена и готова к использованию. 🎉

