/**
 * Скрипт для автоматической настройки системы управленческого учета
 * Использует новую архитектуру согласно техническому заданию
 * 
 * ИСПОЛЬЗОВАНИЕ:
 * 1. Скопируйте все файлы .gs в один проект Google Apps Script
 * 2. Запустите функцию initializeSystem() из меню или напрямую
 * 3. Система создаст все необходимые таблицы и настроит триггеры
 */

/**
 * Упрощенная функция для быстрой настройки (совместимость со старым скриптом)
 */
function setupAccountingSystem() {
  initializeSystem();
}
