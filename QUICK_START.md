# Быстрый старт - Установка за 5 минут

## Минимальная инструкция

### 1. Создайте Google Таблицу
- Откройте [sheets.google.com](https://sheets.google.com)
- Создайте новую таблицу

### 2. Откройте редактор скриптов
- **Расширения** → **Apps Script**

### 3. Удалите файл по умолчанию
- Удалите файл "Код.gs" или "Code.gs"

### 4. Создайте 10 файлов

Нажмите **+** → **Скрипт** для каждого файла и скопируйте код:

| № | Название файла | Код из файла |
|---|----------------|--------------|
| 1 | `Config` | `Config.gs` |
| 2 | `Utils` | `Utils.gs` |
| 3 | `CurrencyManager` | `CurrencyManager.gs` |
| 4 | `VATCalculator` | `VATCalculator.gs` |
| 5 | `PaymentManager` | `PaymentManager.gs` |
| 6 | `DebtCalculator` | `DebtCalculator.gs` |
| 7 | `Notifications` | `Notifications.gs` |
| 8 | `Triggers` | `Triggers.gs` |
| 9 | `TestDataLoader` | `TestDataLoader.gs` |
| 10 | `Main` | `Main.gs` |

### 5. Сохраните проект
- Нажмите **Сохранить** (💾)
- Переименуйте проект

### 6. Запустите инициализацию
- Выберите функцию `initializeSystem`
- Нажмите **Выполнить** (▶️)
- Разрешите доступ

### 7. Загрузите тестовые данные
- Вернитесь в таблицу
- **💼 Управленческий учет** → **🧪 Тестирование** → **Загрузить тестовые данные**

## Готово! ✅

Теперь у вас есть полностью рабочая система с тестовыми данными.

---

**Подробная инструкция:** См. `INSTALLATION_INSTRUCTIONS.md`

