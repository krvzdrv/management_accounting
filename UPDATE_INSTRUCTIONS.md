# Инструкция по обновлению скриптов

## Какие файлы нужно обновить

После исправления ошибки с API нужно обновить **2 файла**:

1. **CurrencyManager** - исправлена функция обновления курсов валют
2. **Main** - добавлена функция ручного добавления курсов через диалог

## Как обновить

### Шаг 1: Откройте редактор скриптов

1. Откройте вашу Google Таблицу
2. Выберите **Расширения** → **Apps Script**

### Шаг 2: Обновите файл CurrencyManager

1. В левой панели найдите файл **CurrencyManager**
2. Откройте его
3. **Удалите весь старый код** из этого файла
4. Откройте файл `CurrencyManager.gs` в вашем проекте
5. Скопируйте **весь новый код** из `CurrencyManager.gs`
6. Вставьте его в файл **CurrencyManager** в редакторе скриптов
7. Нажмите **Сохранить** (💾)

### Шаг 3: Обновите файл Main

1. В левой панели найдите файл **Main**
2. Откройте его
3. Найдите функцию `showAddExchangeRateDialog()` (она должна быть в конце файла)
4. **Замените** старую версию функции на новую:

**Старая версия (удалить):**
```javascript
function showAddExchangeRateDialog() {
  SpreadsheetApp.getUi().alert('Функция в разработке', 'Добавьте курс вручную в лист Exchange_Rates или используйте функцию обновления из API', SpreadsheetApp.getUi().ButtonSet.OK);
}
```

**Новая версия (вставить):**
```javascript
function showAddExchangeRateDialog() {
  const ui = SpreadsheetApp.getUi();
  
  // Получаем список валют
  const currenciesSheet = getSheet(CONFIG.SHEET_NAMES.CURRENCIES);
  const currencies = currenciesSheet.getDataRange().getValues();
  const currencyList = [];
  for (let i = 1; i < currencies.length; i++) {
    currencyList.push(currencies[i][0]);
  }
  
  // Запрашиваем данные
  const fromCurrencyResponse = ui.prompt(
    'Добавить курс валюты',
    'Введите код валюты ОТ которой конвертируем (например: EUR):',
    ui.ButtonSet.OK_CANCEL
  );
  
  if (fromCurrencyResponse.getSelectedButton() !== ui.Button.OK) {
    return;
  }
  
  const fromCurrency = fromCurrencyResponse.getResponseText().toUpperCase().trim();
  
  const toCurrencyResponse = ui.prompt(
    'Добавить курс валюты',
    'Введите код валюты К которой конвертируем (например: PLN):',
    ui.ButtonSet.OK_CANCEL
  );
  
  if (toCurrencyResponse.getSelectedButton() !== ui.Button.OK) {
    return;
  }
  
  const toCurrency = toCurrencyResponse.getResponseText().toUpperCase().trim();
  
  const rateResponse = ui.prompt(
    'Добавить курс валюты',
    `Введите курс (сколько ${toCurrency} за 1 ${fromCurrency}):`,
    ui.ButtonSet.OK_CANCEL
  );
  
  if (rateResponse.getSelectedButton() !== ui.Button.OK) {
    return;
  }
  
  const rate = parseFloat(rateResponse.getResponseText().replace(',', '.'));
  
  if (isNaN(rate) || rate <= 0) {
    ui.alert('Ошибка', 'Некорректное значение курса. Используйте число больше нуля.', ui.ButtonSet.OK);
    return;
  }
  
  // Используем сегодняшнюю дату
  const date = new Date();
  
  try {
    addExchangeRate(fromCurrency, toCurrency, rate, date);
    ui.alert('Успех', `Курс добавлен: ${fromCurrency}/${toCurrency} = ${rate}`, ui.ButtonSet.OK);
  } catch (error) {
    ui.alert('Ошибка', 'Не удалось добавить курс: ' + error.message, ui.ButtonSet.OK);
  }
}
```

5. Нажмите **Сохранить** (💾)

### Шаг 4: Проверьте обновление

1. Вернитесь в Google Таблицу
2. Обновите страницу (F5)
3. Попробуйте:
   - **💼 Управленческий учет** → **💱 Валюты** → **Добавить курс вручную** (должен работать диалог)
   - **💼 Управленческий учет** → **💱 Валюты** → **Обновить курсы (API)** (должна работать с улучшенной обработкой ошибок)

## Что изменилось

### CurrencyManager.gs:
- ✅ Улучшена обработка ошибок API
- ✅ Добавлены альтернативные источники курсов
- ✅ Более понятные сообщения об ошибках
- ✅ Лучшее логирование процесса

### Main.gs:
- ✅ Добавлен полноценный диалог для ручного добавления курсов
- ✅ Проверка корректности ввода
- ✅ Автоматическое добавление в таблицу

## Быстрая проверка

После обновления выполните в редакторе скриптов:

```javascript
// Тест ручного добавления курса
addExchangeRate('EUR', 'PLN', 4.35, new Date());
```

Если работает без ошибок - обновление успешно!

## Если что-то пошло не так

1. **Проверьте логи** (Вид → Логи в редакторе скриптов)
2. **Убедитесь**, что код скопирован полностью
3. **Сохраните** все файлы (Ctrl+S)
4. **Обновите** страницу Google Таблицы

---

**Важно:** Обновление не затронет ваши данные. Все существующие курсы валют останутся на месте.

