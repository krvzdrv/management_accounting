# Архитектура системы хранения и обновления скриптов

## 🏗️ Общая схема системы

```
┌─────────────────────────────────────────────────────────────┐
│                    ВАШ КОМПЬЮТЕР (Mac)                      │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │  GitHub Репозиторий                                  │  │
│  │  /management_accounting/                              │  │
│  │                                                       │  │
│  │  📁 Файлы скриптов:                                  │  │
│  │    • Config.gs                                        │  │
│  │    • Main.gs                                          │  │
│  │    • CurrencyManager.gs                              │  │
│  │    • ... (все .gs файлы)                              │  │
│  │                                                       │  │
│  │  📁 Файлы обновления:                                 │  │
│  │    • update-scripts.js (Node.js)                     │  │
│  │    • update_scripts.py (Python)                      │  │
│  │    • package.json                                     │  │
│  │                                                       │  │
│  │  📁 Конфигурация:                                     │  │
│  │    • .env (секретные данные)                         │  │
│  │    • credentials.json (OAuth)                        │  │
│  │    • token.json (токен доступа)                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                          ↕️                                 │
│                    Git push/pull                           │
└─────────────────────────────────────────────────────────────┘
                          ↕️
                    Интернет
                          ↕️
┌─────────────────────────────────────────────────────────────┐
│              GOOGLE CLOUD (OAuth & API)                    │
│                                                             │
│  • OAuth 2.0 авторизация                                   │
│  • Apps Script API (включен)                              │
│  • Управление доступом                                     │
└─────────────────────────────────────────────────────────────┘
                          ↕️
                    Apps Script API
                          ↕️
┌─────────────────────────────────────────────────────────────┐
│            GOOGLE APPS SCRIPT ПРОЕКТ                       │
│                                                             │
│  📁 Скрипты (синхронизированы с GitHub):                  │
│    • Config.gs                                             │
│    • Main.gs                                               │
│    • CurrencyManager.gs                                    │
│    • ... (все .gs файлы)                                   │
│                                                             │
│  ⚙️ Триггеры:                                              │
│    • Ежедневное обновление курсов валют                    │
│    • Ежедневное обновление балансов                        │
│    • Уведомления о просрочке                               │
└─────────────────────────────────────────────────────────────┘
                          ↕️
                    Связан с
                          ↕️
┌─────────────────────────────────────────────────────────────┐
│              GOOGLE SHEETS ТАБЛИЦА                         │
│                                                             │
│  📊 Листы с данными:                                       │
│    • Sales (Продажи)                                       │
│    • Purchases (Закупки)                                   │
│    • Expenses (Расходы)                                    │
│    • Currencies (Валюты)                                   │
│    • Exchange_Rates (Курсы валют)                          │
│    • ... (все 13 листов)                                   │
│                                                             │
│  📈 Формулы и отчеты:                                      │
│    • Автоматические расчеты                                │
│    • Сводные таблицы                                       │
│    • Отчеты                                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Где что хранится

### 1. **GitHub Репозиторий** (источник истины)

**Что хранится:**
- ✅ Все файлы скриптов (`.gs` файлы)
- ✅ Скрипты обновления (`update-scripts.js`, `update_scripts.py`)
- ✅ Конфигурационные файлы (`package.json`, `requirements.txt`)
- ✅ Документация (`.md` файлы)
- ✅ `.gitignore` (исключает секретные файлы)

**Что НЕ хранится (в `.gitignore`):**
- ❌ `.env` (секретные ключи и токены)
- ❌ `credentials.json` (OAuth credentials)
- ❌ `token.json` (токен доступа)
- ❌ `node_modules/` (зависимости)

**Роль:**
- Версионирование кода (Git)
- Резервная копия
- История изменений
- Совместная работа (если нужно)

---

### 2. **Ваш компьютер (Mac)**

**Что хранится:**
- ✅ Локальная копия GitHub репозитория
- ✅ `.env` файл (секретные данные)
- ✅ `credentials.json` (OAuth credentials)
- ✅ `token.json` (токен доступа)
- ✅ `node_modules/` (зависимости Node.js)

**Роль:**
- Редактирование скриптов
- Запуск скриптов обновления
- Хранение секретных данных (локально, не в Git)

---

### 3. **Google Apps Script Проект**

**Что хранится:**
- ✅ Все `.gs` файлы (рабочие копии)
- ✅ Триггеры (настроенные автоматические задачи)
- ✅ Версия системы (в `VersionManager.gs`)

**Роль:**
- Выполнение скриптов
- Автоматизация (триггеры)
- Интеграция с Google Sheets

**Важно:** Скрипты здесь синхронизируются с GitHub через API, но не напрямую. Обновление происходит через скрипт `update-scripts.js`.

---

### 4. **Google Sheets Таблица**

**Что хранится:**
- ✅ Все данные (продажи, закупки, расходы, валюты и т.д.)
- ✅ Формулы для расчетов
- ✅ Структура листов

**Роль:**
- Хранение данных
- Отображение результатов
- Работа пользователя с данными

**Важно:** Таблица связана с Apps Script проектом. Скрипты читают и записывают данные в таблицу.

---

## 🔄 Как работает обновление

### Процесс обновления скриптов:

```
1. Вы редактируете файлы на компьютере
   ↓
2. Коммитите и пушите в GitHub
   git add .
   git commit -m "Обновление скрипта"
   git push
   ↓
3. Запускаете скрипт обновления
   npm run update
   (или: node update-scripts.js)
   ↓
4. Скрипт делает следующее:
   a) Читает файлы из локальной папки
   b) Авторизуется через OAuth (используя credentials.json)
   c) Отправляет файлы в Apps Script через API
   d) Обновляет каждый .gs файл в проекте
   ↓
5. Apps Script проект обновлен
   ↓
6. Скрипты работают с новой версией в Google Sheets
```

### Детали процесса:

**Авторизация:**
- При первом запуске: открывается браузер, вы авторизуетесь, сохраняется `token.json`
- При последующих запусках: используется сохраненный токен (обновляется автоматически если истек)

**Обновление файлов:**
- Скрипт читает все `.gs` файлы из папки проекта
- Для каждого файла вызывает Apps Script API: `projects.updateContent`
- Файлы обновляются по одному

**Версионирование:**
- В `VersionManager.gs` хранится текущая версия системы
- При обновлении можно запустить миграции (если нужно изменить структуру таблиц)

---

## 🔗 Как все связано

### Связь 1: GitHub ↔ Компьютер
- **Git** синхронизирует код
- Вы делаете `git pull` чтобы получить изменения
- Вы делаете `git push` чтобы отправить изменения

### Связь 2: Компьютер ↔ Google Apps Script
- **Скрипт обновления** (`update-scripts.js`) отправляет файлы через **Apps Script API**
- Используется **OAuth 2.0** для авторизации
- Требуется **credentials.json** и **token.json**

### Связь 3: Google Apps Script ↔ Google Sheets
- Apps Script проект **привязан** к конкретной Google Sheets таблице
- Скрипты используют `SpreadsheetApp.getActiveSpreadsheet()` для доступа к таблице
- Скрипты могут читать/писать данные, создавать листы, запускать формулы

### Связь 4: Google Cloud ↔ Apps Script API
- **Google Cloud проект** предоставляет OAuth credentials
- **Apps Script API** включен в Google Cloud проекте
- Это позволяет внешним скриптам обновлять Apps Script проекты

---

## 📝 Типичный рабочий процесс

### Сценарий 1: Вы хотите изменить скрипт

```
1. Открываете файл на компьютере (например, Config.gs)
2. Редактируете код
3. Сохраняете файл
4. Коммитите в Git:
   git add Config.gs
   git commit -m "Обновил конфигурацию"
   git push
5. Запускаете обновление:
   npm run update
6. Скрипт обновлен в Apps Script
7. Изменения применяются автоматически при следующем запуске
```

### Сценарий 2: Вы хотите добавить новый скрипт

```
1. Создаете новый файл на компьютере (например, NewFeature.gs)
2. Пишете код
3. Добавляете в Git:
   git add NewFeature.gs
   git commit -m "Добавил новую функцию"
   git push
4. Запускаете обновление:
   npm run update
5. Новый файл появляется в Apps Script проекте
6. Можете использовать функции из него в других скриптах
```

### Сценарий 3: Обновление структуры таблиц (миграция)

```
1. Обновляете VersionManager.gs (увеличиваете версию)
2. Добавляете функцию миграции в MigrationScripts.gs
3. Коммитите изменения
4. Запускаете обновление:
   npm run update
5. В Apps Script вызываете функцию обновления:
   (через меню: ⚙️ Настройки → Обновление системы → Обновить систему)
6. Система автоматически:
   - Делает резервную копию
   - Запускает миграцию
   - Обновляет структуру таблиц
```

---

## 🔐 Безопасность

### Что защищено:

1. **Секретные данные в `.env`:**
   - Не попадают в Git (благодаря `.gitignore`)
   - Хранятся только локально на вашем компьютере

2. **OAuth credentials:**
   - `credentials.json` - не в Git
   - `token.json` - не в Git
   - Автоматически обновляется при истечении

3. **Права доступа:**
   - OAuth запрашивает только необходимые права
   - Токен привязан к вашему Google аккаунту

### Рекомендации:

- ✅ Регулярно делайте `git push` (резервная копия кода)
- ✅ Храните `.env` и `credentials.json` в безопасном месте
- ✅ Не делитесь этими файлами ни с кем
- ✅ Используйте `.gitignore` чтобы не закоммитить секреты случайно

---

## 📊 Сравнение: До и После

### ❌ До (старый способ):

```
Редактирование → Копирование кода → Вставка в Apps Script редактор → Сохранение
```

**Проблемы:**
- Нет версионирования
- Нет резервных копий
- Сложно отслеживать изменения
- Ручная работа

### ✅ После (новый способ):

```
Редактирование → Git commit → Git push → npm run update → Автоматическое обновление
```

**Преимущества:**
- ✅ Версионирование (Git)
- ✅ Резервные копии (GitHub)
- ✅ История изменений
- ✅ Автоматизация
- ✅ Безопасность (секреты не в Git)

---

## 🎯 Итоговая схема данных

```
┌──────────────┐
│   GitHub     │ ← Источник истины (код)
└──────┬───────┘
       │ git pull/push
       ↓
┌──────────────┐
│  Компьютер   │ ← Редактирование
└──────┬───────┘
       │ npm run update
       │ (Apps Script API)
       ↓
┌──────────────┐
│ Apps Script  │ ← Выполнение
└──────┬───────┘
       │ SpreadsheetApp
       ↓
┌──────────────┐
│ Google Sheets│ ← Данные
└──────────────┘
```

---

## ❓ Частые вопросы

**Q: Что если я изменю файл напрямую в Apps Script редакторе?**
A: Изменения будут перезаписаны при следующем `npm run update`. Лучше редактировать файлы локально.

**Q: Можно ли работать без GitHub?**
A: Технически да, но тогда не будет версионирования и резервных копий. Рекомендуется использовать GitHub.

**Q: Что если я потеряю `.env` или `credentials.json`?**
A: `.env` можно пересоздать (данные есть в Google Cloud Console). `credentials.json` можно скачать заново из Google Cloud Console.

**Q: Как часто нужно обновлять?**
A: После каждого изменения кода. Можно настроить автоматическое обновление через GitHub Actions (опционально).

**Q: Что если несколько человек работает над проектом?**
A: Каждый работает со своей локальной копией, делает `git pull` перед началом работы, и `git push` после изменений. Обновление в Apps Script делает один человек (или через CI/CD).

---

## 📚 Дополнительные ресурсы

- [SETUP_AUTO_UPDATE.md](SETUP_AUTO_UPDATE.md) - Подробная инструкция по настройке
- [SECURITY.md](SECURITY.md) - Политика безопасности
- [UPDATE_SYSTEM.md](UPDATE_SYSTEM.md) - Система версионирования и миграций

