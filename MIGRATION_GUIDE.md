# Миграция с единого скрипта на модульную систему

## Если вы уже использовали CompleteSetup.gs

Если вы уже запустили `CompleteSetup.gs` и у вас есть все листы, вы можете легко добавить модульную систему для получения дополнительных функций.

## ✅ Хорошие новости

**Все листы уже созданы!** Модульная система будет использовать существующие листы, не пересоздавая их.

## Шаг 1: Удалите старый скрипт (опционально)

1. Откройте редактор скриптов (Расширения → Apps Script)
2. Найдите файл с функцией `setupCompleteSystem`
3. Вы можете:
   - **Вариант А:** Удалить его (если больше не нужен)
   - **Вариант Б:** Оставить его (не мешает, просто не будет использоваться)

## Шаг 2: Добавьте модульные файлы

Добавьте модули в тот же проект Google Apps Script:

### Создайте файлы модулей:

1. Нажмите **+** → **Скрипт** в редакторе скриптов
2. Создайте каждый файл и скопируйте код:

| № | Название файла | Код из файла |
|---|----------------|--------------|
| 1 | `Config` | `Config.gs` |
| 2 | `Utils` | `Utils.gs` |
| 3 | `CurrencyManager` | `CurrencyManager.gs` |
| 4 | `VATCalculator` | `VATCalculator.gs` |
| 5 | `PaymentManager` | `PaymentManager.gs` |
| 6 | `DebtCalculator` | `DebtCalculator.gs` |
| 7 | `Notifications` | `Notifications.gs` |
| 8 | `Triggers` | `Triggers.gs` |
| 9 | `TestDataLoader` | `TestDataLoader.gs` |
| 10 | `Main` | `Main.gs` |

## Шаг 3: Сохраните проект

1. Нажмите **Сохранить** (💾)
2. Все файлы теперь в одном проекте

## Шаг 4: Проверьте работу

### Проверка 1: Меню появилось

1. Вернитесь в Google Таблицу
2. Обновите страницу (F5)
3. Должно появиться меню **💼 Управленческий учет**

### Проверка 2: Функции работают

1. В редакторе скриптов выберите функцию `updateExchangeRatesFromAPI`
2. Нажмите **Выполнить**
3. Если работает без ошибок - все в порядке

## Что изменится?

### ✅ Что останется без изменений:
- Все существующие листы
- Все данные в листах
- Структура таблиц
- Форматирование

### ✨ Что добавится:
- Меню в Google Таблицах
- Функции для работы с данными
- Автоматические триггеры
- Уведомления по email
- Функции расчета задолженностей
- Обновление курсов валют из API

## Важные моменты

### 1. Названия листов должны совпадать

Модульная система ожидает точные названия листов. Проверьте, что у вас есть:

- ✅ Currencies (не "Валюты")
- ✅ Exchange_Rates (не "Курсы валют")
- ✅ Companies (не "Компании")
- ✅ Counterparties (не "Контрагенты")
- И т.д.

Если названия не совпадают, либо:
- Переименуйте листы в Google Таблице
- Или измените названия в `Config.gs` в разделе `SHEET_NAMES`

### 2. Структура колонок

Модульная система ожидает определенную структуру колонок. Если вы изменили структуру после создания единым скриптом, проверьте соответствие.

### 3. Конфликты функций

Если в старом скрипте были функции с такими же именами, как в модульной системе:
- Модульная система перезапишет их
- Это нормально, модульные функции более функциональны

## Пошаговая миграция

### Этап 1: Подготовка (2 минуты)
- [ ] Открыт редактор скриптов
- [ ] Проверены названия листов
- [ ] Сохранена резервная копия (опционально)

### Этап 2: Добавление модулей (10 минут)
- [ ] Создан файл Config
- [ ] Создан файл Utils
- [ ] Создан файл CurrencyManager
- [ ] Создан файл VATCalculator
- [ ] Создан файл PaymentManager
- [ ] Создан файл DebtCalculator
- [ ] Создан файл Notifications
- [ ] Создан файл Triggers
- [ ] Создан файл TestDataLoader
- [ ] Создан файл Main

### Этап 3: Проверка (2 минуты)
- [ ] Проект сохранен
- [ ] Меню появилось в Google Таблицах
- [ ] Функции работают без ошибок

### Этап 4: Настройка (2 минуты)
- [ ] Установлены триггеры (через меню)
- [ ] Проверена работа функций

## Решение проблем

### Проблема: "Sheet not found"

**Причина:** Название листа не совпадает с ожидаемым

**Решение:**
1. Проверьте названия листов в Google Таблице
2. Сравните с названиями в `Config.gs` → `SHEET_NAMES`
3. Либо переименуйте листы, либо измените конфигурацию

### Проблема: Меню не появилось

**Причина:** Файл `Main.gs` не создан или не содержит функцию `onOpen()`

**Решение:**
1. Проверьте, что файл `Main` создан
2. Проверьте, что код из `Main.gs` скопирован полностью
3. Обновите страницу Google Таблицы (F5)
4. Откройте таблицу заново

### Проблема: Ошибки при выполнении функций

**Причина:** Структура данных не соответствует ожидаемой

**Решение:**
1. Проверьте логи (Вид → Логи в редакторе скриптов)
2. Убедитесь, что структура колонок соответствует документации
3. Проверьте, что данные в правильном формате

### Проблема: Конфликт функций

**Причина:** В старом скрипте есть функции с такими же именами

**Решение:**
1. Удалите старый скрипт `CompleteSetup.gs`
2. Или переименуйте конфликтующие функции в старом скрипте

## Обратная миграция

Если вы хотите вернуться к единому скрипту:

1. Удалите все модульные файлы
2. Оставьте только `CompleteSetup.gs`
3. Листы останутся, но функции модульной системы работать не будут

## Рекомендации

### ✅ Рекомендуется:
- Использовать модульную систему для полного функционала
- Оставить старый скрипт как резервную копию (переименовав его)
- Создать резервную копию проекта перед миграцией

### ⚠️ Не рекомендуется:
- Удалять данные при миграции
- Изменять структуру листов во время миграции
- Запускать функции до завершения миграции

## Проверочный список после миграции

После добавления всех модулей проверьте:

- [ ] Все 10 модульных файлов созданы
- [ ] Проект сохранен
- [ ] Меню "💼 Управленческий учет" появилось
- [ ] Функция `updateExchangeRatesFromAPI` работает
- [ ] Функция `updateAllAccountBalances` работает
- [ ] Триггеры установлены
- [ ] Нет ошибок в логах

## Готово! ✅

После миграции у вас будет:
- ✅ Все существующие листы и данные
- ✅ Все функции модульной системы
- ✅ Меню в Google Таблицах
- ✅ Автоматические триггеры
- ✅ Расширенный функционал

---

**Вопросы?** См. [INSTALLATION_INSTRUCTIONS.md](INSTALLATION_INSTRUCTIONS.md) для подробной информации о модульной системе.

