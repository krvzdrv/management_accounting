# Быстрое исправление ошибки API

## Проблема
Ошибка: `Error: API returned unsuccessful response` на строке 74

## Решение за 2 минуты

### Шаг 1: Откройте редактор скриптов
1. В Google Таблице: **Расширения** → **Apps Script**

### Шаг 2: Найдите файл CurrencyManager
1. В левой панели найдите файл **CurrencyManager**
2. Кликните на него

### Шаг 3: Найдите функцию updateExchangeRatesFromAPI
1. Нажмите `Ctrl+F` (или `Cmd+F` на Mac)
2. Введите: `updateExchangeRatesFromAPI`
3. Найдите эту функцию (она должна быть около строки 66)

### Шаг 4: Замените функцию
1. Найдите строку с `throw new Error('API returned unsuccessful response');`
2. **Удалите эту строку** (если она есть)
3. Или просто **замените весь файл CurrencyManager** на новый код из `CurrencyManager.gs`

### Шаг 5: Сохраните
1. Нажмите **Сохранить** (💾) или `Ctrl+S`

### Шаг 6: Проверьте
1. Выберите функцию `updateExchangeRatesFromAPI` в выпадающем списке
2. Нажмите **Выполнить** (▶️)
3. Теперь функция должна работать без ошибок

## Альтернатива: Используйте ручное добавление

Если API не работает, просто добавляйте курсы вручную:

1. Меню → **💼 Управленческий учет** → **💱 Валюты** → **Добавить курс вручную**
2. Введите данные:
   - От: EUR
   - К: PLN  
   - Курс: 4.35

## Что изменилось

**Старая версия:** Выбрасывала ошибку при проблемах с API

**Новая версия:** 
- ✅ Логирует проблемы, но не выбрасывает ошибку
- ✅ Пробует альтернативные API
- ✅ Показывает пользователю понятное сообщение
- ✅ Предлагает добавить курсы вручную

---

**Важно:** Обновите файл CurrencyManager в Google Apps Script, чтобы получить исправления!

