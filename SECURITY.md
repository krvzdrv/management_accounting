# Безопасность проекта

## 🔒 Политика безопасности

Этот проект содержит скрипты для работы с чувствительными данными (OAuth credentials, токены доступа). Следуйте этим правилам для обеспечения безопасности.

## ✅ Что сделано для безопасности

### 1. Исключение чувствительных файлов из Git

Все чувствительные файлы добавлены в `.gitignore`:
- `.env` и все варианты `.env.*`
- `credentials.json`
- `token.json`, `token.pickle`
- Все файлы с токенами и ключами

### 2. Использование переменных окружения

Все чувствительные данные хранятся в `.env` файле, который:
- ✅ Не коммитится в Git
- ✅ Используется только локально
- ✅ Имеет шаблон `.env.example` без реальных данных

### 3. Ограничение прав доступа

Токены сохраняются с правами доступа только для владельца:
- Unix/Linux/Mac: `600` (только владелец может читать/писать)
- Windows: стандартные права пользователя

### 4. Проверка безопасности

Скрипт `security-check.js` проверяет:
- ✅ Что чувствительные файлы в `.gitignore`
- ✅ Что файлы не отслеживаются в Git
- ✅ Что в коде нет захардкоженных credentials
- ✅ Что права доступа к файлам правильные

## ⚠️ Что НЕ делать

### ❌ НИКОГДА не делайте:

1. **Не коммитьте `.env` файл**
   ```bash
   # Плохо:
   git add .env
   git commit -m "Add config"
   
   # Хорошо:
   git add .env.example
   ```

2. **Не коммитьте `credentials.json`**
   - Этот файл содержит OAuth credentials
   - Используйте `.env.example` как шаблон

3. **Не коммитьте `token.json`**
   - Токены доступа должны быть только локально
   - Они автоматически создаются при первом запуске

4. **Не храните credentials в коде**
   ```javascript
   // Плохо:
   const CLIENT_ID = "123456789-abc.apps.googleusercontent.com";
   
   // Хорошо:
   const CLIENT_ID = process.env.GOOGLE_CLIENT_ID;
   ```

5. **Не делитесь credentials**
   - Не отправляйте `.env` по email
   - Не публикуйте в чатах или форумах
   - Используйте безопасные каналы для передачи

## 🔍 Проверка безопасности

### Запуск проверки:

```bash
node security-check.js
```

### Что проверяется:

1. ✅ `.gitignore` содержит все нужные файлы
2. ✅ Чувствительные файлы не отслеживаются в Git
3. ✅ В коде нет захардкоженных credentials
4. ✅ Права доступа к файлам правильные

## 🛡️ Рекомендации

### 1. Используйте `.env.example` как шаблон

```bash
# Создайте .env из шаблона
cp .env.example .env

# Заполните реальными значениями
nano .env  # или ваш редактор
```

### 2. Регулярно проверяйте безопасность

```bash
# Перед каждым коммитом
node security-check.js

# Перед push в Git
git status  # Убедитесь что нет .env в staged files
```

### 3. Ротация credentials

Если credentials скомпрометированы:
1. Отзовите старые credentials в Google Cloud Console
2. Создайте новые OAuth credentials
3. Обновите `.env` файл
4. Удалите старый `token.json` и авторизуйтесь заново

### 4. Используйте разные credentials для разных сред

- **Development**: локальные credentials
- **Production**: отдельные credentials с ограниченными правами

## 📋 Чек-лист безопасности

Перед коммитом проверьте:

- [ ] `.env` не в staged files (`git status`)
- [ ] `credentials.json` не в staged files
- [ ] `token.json` не в staged files
- [ ] В коде нет захардкоженных credentials
- [ ] `security-check.js` проходит без ошибок
- [ ] `.gitignore` содержит все нужные паттерны

## 🚨 Если credentials скомпрометированы

1. **Немедленно отзовите credentials:**
   - Google Cloud Console → APIs & Services → Credentials
   - Удалите или отзовите скомпрометированные credentials

2. **Создайте новые credentials:**
   - Создайте новый OAuth Client ID
   - Обновите `.env` файл

3. **Удалите старые токены:**
   ```bash
   rm token.json token.pickle
   ```

4. **Авторизуйтесь заново:**
   ```bash
   npm run update  # или python update_scripts.py
   ```

5. **Проверьте логи доступа:**
   - Google Cloud Console → APIs & Services → Credentials
   - Проверьте историю использования

## 📚 Дополнительные ресурсы

- [Google OAuth 2.0 Security Best Practices](https://developers.google.com/identity/protocols/oauth2/security-best-practices)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Git Security Best Practices](https://git-scm.com/docs/git-config#_security)

## 🔐 Контакты для вопросов безопасности

Если вы обнаружили уязвимость безопасности:
1. НЕ создавайте публичный issue
2. Свяжитесь с разработчиком напрямую
3. Опишите проблему детально

