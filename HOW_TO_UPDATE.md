# Как обновлять скрипты (без ручного копирования)

## 🎯 Быстрый способ (рекомендуется)

### Простая команда:

```bash
cd "/Volumes/02 Data/work/Alumineu/GitHub/management_accounting"
./quick-update.sh
```

Этот скрипт:
- ✅ Проверяет настройки
- ✅ Обновляет скрипты через Apps Script API
- ✅ Не требует ручного копирования

---

## 📋 Полный процесс обновления

### Вариант 1: Одна команда (самый простой)

```bash
cd "/Volumes/02 Data/work/Alumineu/GitHub/management_accounting"
./quick-update.sh
```

### Вариант 2: Две команды

```bash
cd "/Volumes/02 Data/work/Alumineu/GitHub/management_accounting"
npm run update
```

---

## 🔄 Типичный рабочий процесс

### 1. Вы редактируете код

Открываете файл на компьютере, например `Config.gs`, редактируете и сохраняете.

### 2. Обновляете скрипты (одна команда)

```bash
./quick-update.sh
```

**Всё!** Скрипты обновлены в Apps Script, изменения работают в Google Sheets.

### 3. Опционально: Сохраняете в Git

```bash
git add .
git commit -m "Обновил конфигурацию"
git push
```

---

## 🚀 Автоматизация через GitHub Actions (опционально)

Если хотите полностью автоматизировать обновление при push в GitHub:

### Настройка GitHub Actions

1. **На GitHub:**
   - Откройте ваш репозиторий: https://github.com/krvzdrv/management_accounting
   - Settings → Secrets and variables → Actions
   - Добавьте секреты:
     - `GOOGLE_CLIENT_ID` - из .env
     - `GOOGLE_CLIENT_SECRET` - из .env
     - `GOOGLE_SCRIPT_ID` - из .env
     - `GOOGLE_CREDENTIALS` - содержимое credentials.json (весь файл)
     - `GOOGLE_TOKEN` - содержимое token.json (если есть)

2. **Файл `.github/workflows/update-apps-script.yml` уже создан**

3. **После настройки:**
   - Каждый push в `main` автоматически обновляет Apps Script
   - Больше не нужно запускать `npm run update` вручную

**Важно:** GitHub Actions требует настройки секретов один раз, потом работает автоматически.

---

## ❓ Вопросы

### Q: Нужно ли каждый раз запускать скрипт вручную?

**A:** Да, но это одна команда: `./quick-update.sh`

После настройки GitHub Actions - нет, обновление происходит автоматически при push.

### Q: Можно ли обновлять через меню в Google Sheets?

**A:** Нет, технически невозможно:
- Apps Script API требует OAuth credentials (секреты)
- Эти секреты нельзя хранить в коде Apps Script
- Обновление скриптов должно происходить через локальный компьютер

**Но:** Обновление структуры таблиц работает через меню ("Обновить систему").

### Q: Что если я изменю код напрямую в Apps Script редакторе?

**A:** Изменения будут потеряны при следующем обновлении. Лучше редактировать файлы локально и обновлять через скрипт.

### Q: Как часто нужно обновлять?

**A:** После каждого изменения кода. Это одна команда, занимает 10-30 секунд.

---

## 📝 Резюме

**Самый простой способ обновления:**

```bash
./quick-update.sh
```

**Одна команда, никакого ручного копирования!** ✅
