# Руководство по внедрению системы управленческого учета

## Структура проекта

Система состоит из следующих модулей Google Apps Script:

### Основные модули:
1. **Config.gs** - Конфигурация системы и константы
2. **Utils.gs** - Вспомогательные функции
3. **CurrencyManager.gs** - Управление валютами и курсами
4. **VATCalculator.gs** - Расчеты НДС
5. **PaymentManager.gs** - Управление платежами
6. **DebtCalculator.gs** - Расчет задолженностей
7. **Notifications.gs** - Уведомления по email
8. **Triggers.gs** - Настройка автоматических триггеров
9. **Main.gs** - Главное меню и инициализация системы
10. **TestDataLoader.gs** - Загрузка тестовых данных для проверки системы
11. **setup_script.gs** - Совместимость со старым скриптом

## Установка

### Шаг 1: Создание Google Таблицы
1. Создайте новую Google Таблицу
2. Дайте ей название, например "Управленческий учет Alumineu"

### Шаг 2: Открытие редактора скриптов
1. В меню выберите **Расширения** → **Apps Script**
2. Откроется редактор скриптов

### Шаг 3: Добавление модулей
1. Удалите код по умолчанию (функция `myFunction`)
2. Создайте файлы для каждого модуля:
   - Нажмите **+** рядом с "Файлы"
   - Выберите "Скрипт"
   - Переименуйте файл (например, `Config`)
   - Скопируйте код из соответствующего файла `.gs`

3. Повторите для всех модулей:
   - Config.gs → Config
   - Utils.gs → Utils
   - CurrencyManager.gs → CurrencyManager
   - VATCalculator.gs → VATCalculator
   - PaymentManager.gs → PaymentManager
   - DebtCalculator.gs → DebtCalculator
   - Notifications.gs → Notifications
   - Triggers.gs → Triggers
   - TestDataLoader.gs → TestDataLoader
   - Main.gs → Main

### Шаг 4: Сохранение проекта
1. Нажмите **Сохранить** (Ctrl+S / Cmd+S)
2. Дайте проекту название: "Management Accounting System"

### Шаг 5: Инициализация системы
1. В редакторе скриптов выберите функцию `initializeSystem` из выпадающего списка
2. Нажмите **Выполнить** (▶️)
3. При первом запуске Google запросит разрешения:
   - Нажмите **Проверить разрешения**
   - Выберите свой аккаунт
   - Нажмите **Дополнительно** → **Перейти к [название проекта] (небезопасно)**
   - Нажмите **Разрешить**

4. Система создаст все необходимые таблицы

### Шаг 6: Настройка начальных данных

#### Добавьте компании в лист Companies:
```
company_id | company_code | company_name | tax_number | country_code | is_active
1          | ALUM         | Alumineu    |            | PL           | TRUE
2          | VERS         | Versatrade  |            | PL           | TRUE
```

#### Добавьте валюты в лист Currencies:
```
currency_code | currency_name | symbol | is_base_currency | is_active
EUR           | Евро          | €      | TRUE             | TRUE
PLN           | Злотый        | zł     | FALSE            | TRUE
```

#### Добавьте контрагентов в лист Counterparties:
```
counterparty_id | counterparty_code | counterparty_name | counterparty_type | ...
1               | FLEXY             | Flexy             | supplier          | ...
2               | MEDIATOR          | Посредник         | supplier          | ...
```

#### Добавьте ставки НДС в лист VAT_Rates:
```
vat_id | vat_rate | vat_description | country_code | valid_from | valid_to | is_active
1      | 23       | Стандартная     | PL           | 2024-01-01 |          | TRUE
2      | 8        | Пониженная      | PL           | 2024-01-01 |          | TRUE
```

### Шаг 7: Настройка триггеров
1. В Google Таблице откройте меню **💼 Управленческий учет** → **⚙️ Настройки** → **Установить триггеры**
2. Или запустите функцию `setupAllTriggers()` из редактора скриптов

### Шаг 8: Загрузка тестовых данных (опционально)
1. В Google Таблице откройте меню **💼 Управленческий учет** → **🧪 Тестирование** → **Загрузить тестовые данные**
2. Или запустите функцию `loadTestData()` из редактора скриптов
3. Это добавит:
   - 3 компании (Alumineu, Versatrade, Flexy)
   - 6 контрагентов (3 поставщика, 3 клиента)
   - 10 товаров (алюминиевые профили и фурнитура)
   - 7 категорий расходов
   - 3 закупки с разными статусами оплаты
   - 3 продажи (включая одну в PLN)
   - 5 расходов
   - 5 движений денежных средств
   - 4 платежа
   
   Все данные связаны между собой и готовы для тестирования отчетов и расчетов.

## Использование системы

### Меню системы
После инициализации в Google Таблицах появится меню **💼 Управленческий учет** с разделами:

- **📊 Справочники** - управление справочниками
- **💱 Валюты** - обновление курсов валют
- **💰 Задолженности** - расчет и проверка задолженностей
- **⚙️ Настройки** - настройка системы и триггеров

### Работа с данными

#### Создание закупки:
1. В листе **Purchase_Orders** добавьте новую строку с заголовком заказа
2. В листе **Purchase_Order_Lines** добавьте строки товаров, указав `po_id`

#### Создание продажи:
1. В листе **Sales_Orders** добавьте новую строку с заголовком заказа
2. В листе **Sales_Order_LINES** добавьте строки товаров, указав `so_id`

#### Регистрация платежа:
Используйте функцию `registerPayment()` через меню или напрямую:

```javascript
registerPayment({
  company_id: 1,
  counterparty_id: 3,
  document_type: 'purchase',
  document_id: 1,
  payment_date: new Date(),
  currency_code: 'EUR',
  payment_amount: 1000.00,
  payment_method: 'bank_transfer',
  notes: 'Оплата заказа PO-001'
});
```

### Автоматические функции

#### Обновление курсов валют:
- Автоматически каждый день в 9:00
- Или вручную через меню **💱 Валюты** → **Обновить курсы (API)**

#### Обновление балансов:
- Автоматически каждый день в 10:00
- Или вручную через меню **💰 Задолженности** → **Обновить балансы**

#### Проверка просроченной задолженности:
- Автоматически каждый день в 11:00
- Отправляет email с уведомлениями

## Структура таблиц

### Справочники:
- **Currencies** - валюты
- **Exchange_Rates** - курсы валют
- **VAT_Rates** - ставки НДС
- **Companies** - компании группы
- **Counterparties** - контрагенты
- **Products** - товары
- **Expense_Categories** - категории расходов

### Транзакционные таблицы:
- **Purchase_Orders** - заголовки закупок
- **Purchase_Order_Lines** - строки закупок
- **Sales_Orders** - заголовки продаж
- **Sales_Order_LINES** - строки продаж
- **Expenses** - расходы
- **Cash_Transactions** - движение денежных средств
- **Payments** - платежи
- **Intercompany_Loans** - займы между компаниями
- **Intercompany_Loan_Payments** - погашение займов

### Отчетные таблицы:
- **Account_Balances** - балансы по контрагентам (автоматически обновляется)

## Важные замечания

1. **ID должны быть уникальными** - система автоматически генерирует следующие ID
2. **Даты** - используйте формат даты Google Sheets
3. **Валюты** - все суммы автоматически конвертируются в базовую валюту (EUR)
4. **НДС** - хранится отдельно, но в отчетах показывается без НДС
5. **Триггеры** - настраиваются автоматически при инициализации
6. **Разделители в формулах** - в Google Таблицах используйте точку с запятой (;) как разделитель параметров функций:
   - Правильно: `=SUMIF(A:A; "значение"; B:B)`
   - Неправильно: `=SUMIF(A:A, "значение", B:B)`
   - Исключение: внутри SQL запросов QUERY запятые остаются (это SQL синтаксис)

## Решение проблем

### Ошибка "Sheet not found"
- Убедитесь, что все таблицы созданы через `initializeSystem()`
- Проверьте названия таблиц в CONFIG.SHEET_NAMES

### Ошибка при обновлении курсов валют
- Проверьте подключение к интернету
- API может быть временно недоступен, используйте ручной ввод курсов

### Триггеры не работают
- Проверьте, что триггеры установлены через `setupAllTriggers()`
- Убедитесь, что у скрипта есть необходимые разрешения

### Формулы не обновляются
- Убедитесь, что в таблицах есть данные
- Проверьте правильность ссылок на другие листы

## Дополнительная настройка

### Изменение базовой валюты:
В файле `Config.gs` измените:
```javascript
BASE_CURRENCY: 'EUR'  // на нужную валюту
```

### Изменение времени триггеров:
В файле `Triggers.gs` измените `.atHour()` для нужного времени

### Добавление новых валют:
1. Добавьте валюту в лист **Currencies**
2. Курсы будут обновляться автоматически через API

## Поддержка

При возникновении проблем:
1. Проверьте логи в редакторе скриптов (Вид → Логи)
2. Убедитесь, что все модули скопированы правильно
3. Проверьте разрешения скрипта

